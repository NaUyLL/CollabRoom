"""CollabRoom 安装配置"""
from setuptools import setup, find_packages

setup(
    name="collabroom",
    version="0.1.0",
    description="多 Agent 协作框架 — 让多个 AI Agent 在一个房间里协作交流",
    author="NaUyLL",
    packages=find_packages(),
    python_requires=">=3.10",
    install_requires=[],
)
